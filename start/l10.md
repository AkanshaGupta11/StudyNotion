NAVBAR 

main.jsx -> <NavBar/>

    NAVBAR 
top level div 


jho page mai ho vho yellow -. bakki diff color 
path or current loc match --> yellow color , white color 
path aaya -> navbar links se -> tho jb render kr rhe tho -> NavBar.path bhej do  -> ab y path compared with current location k path se 

//location k liye --> conat location = useLocation 
const matchRoute = (route) => {
    return matchpath({path:route},location.pathname)
}

$matchpath(link.path)?"":""

//login / signup /dashboard ------------------
hoskta user phele se logged in ho , or usne items cart mai add kiye hain -> tho cho cart dikhna chahiye 
logged in nhi hain -> login / signup btn 
logged in -> profile dikh rhi 

cart k button instructor ko nhi dikhana 

HOW TO GET KI USER LOGGED IN KI NHI 
token -- nul -> not logged in -> logged in or signup btn 
token -- !null -> profile dikaho 
user && user !instuctor --> cart dikhana pdega -> ab cart mai kitne item kese pt a-> data data \
STATE MANAGEMENT 

main.jsx ->provider{store = {store}}

const store = configurestore({
    reducer:rootreducer,
})

rootreducer --> sareee slices ko collect krns 
REDUCER -> index.js 
import combinereducer

const rootReducer = combinereducer({
    aut:authRecer ,

})


slice create krte time --> initial state -->
value --> local storage se pivk val 

//jho jho info lg rhi vho rakho baaki waapis 
token :localStorage.getItem()

store bnao 
const store = configurestore({
    reducer:rootreducer
})

const rootreducer = combinereducer({
    auth :authreducer,
    profile,

})

const initialstata = ({
    token :
})
const authslice = createslice({
    name ,
    initialstate,
    reducer :{
        settoken(state,action){

        }
    }
})


cartSlice 
const initialState = ({
    totalItems = local 
})

const cartSlice = createSlice ({

})


user && user.accType != Instructor &&(
    link to 
    {
        totalitem > 0  && {
            spn {totalitem}
        }
    }
)

loin btn dikhana ki nhi 
token === null && (
    link to "/login"
    button login 
)

token == null &7 link -> l


show all category krna hain --> data tho backend se aayega -- api connect krna pdega 

apiconnector --> axios 
create method se koi bhi req -> get , post 

api.js -> konsi link pai call krne jaana hia 
 api call -> to fetch category --> 

 const {sublink , setSublink }

 useEffect (() => {
    async() => {
        try{
            result = await apiGenerator("GET",)
        }
        catch(error) {

        }
    }
 })

 